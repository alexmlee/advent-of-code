import { processInputAndExecuteCallback } from "./util";

async function day_N_solution() {
  const callback = (line) => {};
  await processInputAndExecuteCallback("day_.test-input.txt", callback);
}

day_N_solution();
